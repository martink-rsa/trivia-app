## JSDocs example:

```js
/**
 *
 * This is a description of the function and what it will be doing. The
 * line can run over to this next line.
 *
 * @param {number} value The value that is going to be displayed
 * @param {number} [decimalPlaces=2] A default value can be mentioned
 * @returns {string} What gets returned from the function
 * @example
 * // returns "1.00"
 * myFn(1, 2)
 *
 */
```
