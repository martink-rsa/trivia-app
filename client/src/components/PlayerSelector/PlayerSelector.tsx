import { useState } from 'react';
import * as S from './PlayerSelector.style';

function PlayerSelector() {
  const [currentIcon, setCurrentIcon] = useState(0);

  return <div>Player selector</div>;
}

export default PlayerSelector;
